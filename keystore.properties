# Release signing (committed for CI; needed so updates install over old builds)
storePassword=pocketvmpass123
keyPassword=pocketvmpass123
keyAlias=pocketvm
storeFile=keystore/pocketvm-release.keystore
